女儿的构思，儿子的设计，老爹的口喷，AI的执行完成的FPS Game Demo

# Three.js FPS Game

> 使用 Three.js 和 Cannon-es 构建的的第一人称射击游戏

[![TypeScript](https://img.shields.io/badge/TypeScript-5.3-blue)](https://www.typescriptlang.org/)
[![Three.js](https://img.shields.io/badge/Three.js-0.160-black)](https://threejs.org/)
[![Vite](https://img.shields.io/badge/Vite-5.0-purple)](https://vitejs.dev/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

## 项目概述

这是一个完整的 **3D 第一人称射击 (FPS) 游戏**，使用现代 Web 技术构建。项目具有清晰的模块化架构、完整的游戏系统和良好的性能优化，展示了使用 TypeScript 和 Three.js 构建复杂 3D 游戏的最佳实践。

### 核心特性

- **完整 FPS 体验** - 第一人称视角、流畅的移动控制、精确的射击系统
- **7 种武器** - 手枪、步枪、霰弹枪、冲锋枪、狙击枪、火箭发射器、火焰喷射器
- **智能敌人 AI** - 4 种敌人类型，带有状态机、队友感知、躲避和远程射击能力
- **物理引擎** - 使用 Cannon-es 实现真实的物理模拟和碰撞检测
- **程序化关卡** - 动态生成的游戏场景，包含平台、障碍物和走廊
- **粒子效果** - 枪口火焰、爆炸、伤害数字等视觉反馈
- **3D 音效** - 空间音频、武器音效、背景音乐

### 抶术亮点

- **事件驱动架构** - EventBus 解耦系统间通信
- **依赖注入** - DIContainer 自动管理服务依赖
- **空间分区优化** - SpatialGrid 实现 O(1) 实体查询
- **对象池** - 减少垃圾回收，提升性能
- **协调器系统** - 职责分离的模块化设计

---

## 快速开始

### 环境要求

- Node.js >= 18
- npm >= 9

### 安装

```bash
# 克隆项目
git clone https://github.com/lyzhuzh/game2026.git
cd game2026

# 安装依赖
npm install
```

### 开发

```bash
# 启动开发服务器
npm run dev

# 访问 http://localhost:3000
# 点击页面开始游戏（会初始化音频并锁定鼠标）
```

### 构建

```bash
# 生产构建
npm run build

# 预览构建结果
npm run preview
```

---

## 游戏操作

### 移动控制

| 按键 | 功能 |
|------|------|
| `W A S D` | 移动（前/后/左/右） |
| `空格` | 跳跃 |
| `左 Ctrl` | 蹲伏 |
| `左 Shift` | 冲刺 |
| `鼠标` | 视角控制 |

### 战斗操作

| 按键 | 功能 |
|------|------|
| `鼠标左键` | 攻击 |
| `鼠标右键` | 瞄准（狙击镜） |
| `R` | 换弹 |
| `1 - 7` | 切换武器 |
| `鼠标滚轮` | 上下切换武器 |

### 其他操作

| 按键 | 功能 |
|------|------|
| `E` | 交互（拾取物品） |
| `ESC` | 暂停 |
| `Tab` | 切换库存 |
| `V` | 切换视角 |
| `H` | ANT-DEBUG 帮助 |

### 武器列表

| 槽位 | 武器 | 类型 |
|------|------|------|
| 1 | 手枪 | 半自动 |
| 2 | 步枪 | 全自动 |
| 3 | 霰弹枪 | 多发弹丸 |
| 4 | 冲锋枪 (SMG) | 高射速 |
| 5 | 狙击枪 | 单发高伤害 + 瞄准镜 |
| 6 | 火箭发射器 | 投射物 + 范围爆炸 |
| 7 | 火焰喷射器 | 锥形持续伤害 |

---

## 项目结构

```
src/
├── core/                 # 核心系统
│   ├── Game.ts          # 游戏主类（单例）
│   ├── GameLoop.ts      # 固定时间步长游戏循环
│   ├── EventBus.ts      # 事件总线系统
│   ├── DebugTools.ts    # 内置调试工具 (ANT-DEBUG)
│   └── coordinators/    # 协调器系统
│       ├── BaseCoordinator.ts
│       ├── PhysicsCoordinator.ts
│       ├── CombatCoordinator.ts
│       ├── AICoordinator.ts
│       ├── RenderCoordinator.ts
│       └── CoordinatorManager.ts
├── player/              # 玩家系统
│   ├── Player.ts        # 玩家状态管理
│   ├── FirstPersonCamera.ts
│   └── MovementController.ts
├── physics/             # 物理系统
│   ├── PhysicsWorld.ts  # 物理世界管理
│   ├── CharacterController.ts
│   └── SpatialGrid.ts   # 空间分区优化
├── weapons/             # 武器系统
│   ├── WeaponManager.ts
│   ├── HitscanWeapon.ts
│   └── ProjectileWeapon.ts
├── enemies/             # 敌人系统
│   ├── Enemy.ts         # 敌人基类（状态机）
│   └── EnemyManager.ts
├── items/               # 物品系统
│   └── ItemManager.ts   # 生命值、护甲、弹药
├── particles/           # 粒子系统
│   ├── ParticleSystem.ts
│   └── presets/        # 粒子预设
├── ui/                  # 用户界面
│   ├── UIManager.ts
│   └── components/     # HUD 组件
├── audio/               # 音频系统
│   └── SoundManager.ts  # 音频管理（单例）
├── assets/              # 资源管理
│   ├── AssetManager.ts  # 资源管理（单例）
│   └── AssetConfig.ts   # 资源配置
├── level/               # 关卡系统
│   └── LevelBuilder.ts  # 程序化关卡生成
├── input/               # 输入系统
│   ├── InputManager.ts  # 输入管理（单例）
│   └── InputBindings.ts # 动作映射
├── config/              # 配置系统
│   └── GameConfig.ts    # 统一配置管理
└── utils/               # 工具类
    ├── TimerManager.ts  # 定时器管理
    ├── PerformanceMonitor.ts
    ├── DIContainer.ts   # 依赖注入
    └── ObjectPool.ts    # 对象池基类
```

---

## 技术栈

### 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| [Three.js](https://threejs.org/) | ^0.160.0 | 3D 渲染引擎 |
| [Cannon-es](https://github.com/pmndrs/cannon-es) | ^0.20.0 | 物理引擎 |
| [TypeScript](https://www.typescriptlang.org/) | ^5.3.0 | 类型安全开发 |
| [Vite](https://vitejs.dev/) | ^5.0.0 | 快速构建工具 |

### 开发工具

| 工具 | 用途 |
|------|------|
| puppeteer | Mixamo 动画下载 |
| ts-node | 直接运行 TypeScript 脚本 |

---

## 架构设计

项目采用现代化的架构设计模式，确保代码的可维护性和可扩展性。

### 核心架构模式

#### 1. 事件驱动架构

使用 **EventBus** 实现系统间解耦通信：

```typescript
import { EventBus, GameEvents } from '@/core/EventBus';

// 监听事件
eventBus.on(GameEvents.ENEMY_DIED, (data) => {
    console.log('Enemy died:', data.scoreValue);
});

// 触发事件
eventBus.emit(GameEvents.ENEMY_DIED, { scoreValue: 100 });
```

#### 2. 依赖注入

**DIContainer** 自动管理服务依赖和生命周期：

```typescript
import { DIContainer } from '@/utils/DIContainer';

const container = new DIContainer();

// 注册服务
container.register('physics', () => new PhysicsWorld());
container.register('enemies', (physics) => new EnemyManager(physics), ['physics']);

// 解析服务
const enemies = container.resolve<EnemyManager>('enemies');
```

#### 3. 协调器系统

将 Game 类职责拆分为专门的协调器：

- **PhysicsCoordinator** - 物理模拟协调
- **CombatCoordinator** - 战斗逻辑协调
- **AICoordinator** - AI 行为协调
- **RenderCoordinator** - 渲染协调

#### 4. 空间分区优化

**SpatialGrid** 实现 O(1) 实体查询：

```typescript
import { SpatialGrid } from '@/physics/SpatialGrid';

const grid = new SpatialGrid(10); // 10 单元格大小

// 插入实体
grid.insert({ id: 1, position: new THREE.Vector3(0, 0, 0) });

// 查询附近实体
const nearby = grid.queryNearby(playerPos, 30);
```

#### 5. 统一配置管理

**GAME_CONFIG** 集中管理所有游戏配置：

```typescript
import { GAME_CONFIG, AUDIO_CONFIG } from '@/config/GameConfig';

// 类型安全的配置访问
const footstepInterval = AUDIO_CONFIG.footstepInterval;
const playerSpeed = GAME_CONFIG.player.movement.walkSpeed;
```

---

## 性能优化

项目实现了多项性能优化措施：

| 优化项 | 实现方式 | 性能提升 |
|--------|----------|----------|
| 射线检测 | Cannon-es 内置 Raycast | 5-10x |
| 实体查询 | SpatialGrid 空间分区 | 10-100x |
| 内存管理 | TimerManager 统一管理 | 消除泄漏 |
| 对象复用 | ObjectPool 对象池 | 减少 GC |
| 代码分割 | Three.js/Cannon-es 独立 chunk | 缓存优化 |

---

## 内置调试工具 (ANT-DEBUG)

按 `H` 键查看帮助，游戏内置了强大的 3D 场景测量工具：

### 6 种测量模式

| 模式 | 快捷键 | 功能 |
|------|--------|------|
| POINT | `1` | 单点标记 |
| BOUNDS | `2` | 4 点矩形边界测量 |
| DISTANCE | `3` | 2 点距离测量 |
| PATH | `4` | 多点路径标记 |
| AREA | `5` | 多边形面积计算 |
| BOX | `6` | 2 点盒子测量 |

### 操作快捷键

- `M` - 标记点
- `C` - 清除所有标记
- `U` - 撤销上一步
- `P` - 打印测量摘要

---

## 部署

### Cloudflare Pages 部署

项目已配置好 Cloudflare Pages 部署，详细步骤请查看 [部署指南](docs/DEPLOYMENT.md)。

### 快速部署

1. 推送代码到 GitHub
2. Cloudflare Pages 连接 GitHub 仓库
3. 配置构建设置：
   - 构建命令：`npm run build`
   - 输出目录：`dist`
4. 自动部署完成

### 构建优化

- ✅ 关闭生产环境 sourcemap
- ✅ Three.js 和 Cannon-es 代码分割
- ✅ 静态资源长期缓存策略
- ✅ 单页应用路由重定向

---

## 资源下载

### 武器模型

下载 [Kenney Blaster Kit](https://kenney.nl/assets/blaster-kit)

### 环境模块

下载 [Kenney Space Kit](https://kenney.nl/assets/space-kit)

### 敌人模型

从 [Sketchfab - Quaternius](https://sketchfab.com/quaternius) 下载免费模型

详细步骤请查看 [assets/README.md](assets/README.md)

---

## 开发指南

### 添加新武器

1. 在 `src/weapons/` 创建武器类
2. 在 `src/assets/AssetConfig.ts` 添加模型配置
3. 在 `WeaponManager` 中注册武器
4. 在 `GAME_CONFIG` 中添加武器配置

### 添加新敌人类型

1. 在 `src/enemies/Enemy.ts` 中添加敌人配置
2. 在 `EnemyManager.ts` 中配置生成逻辑
3. 在 `GAME_CONFIG` 中添加属性配置

### 添加新事件

1. 在 `src/core/EventBus.ts` 中定义事件
2. 在需要的地方触发事件
3. 在协调器中监听事件并响应

---

## 常见问题

### Q: 游戏无法启动？

A: 确保点击了页面开始按钮，这会初始化音频上下文并锁定鼠标指针。

### Q: 性能卡顿？

A: 检查浏览器控制台是否有错误，尝试关闭浏览器其他标签页，或降低游戏画质。

### Q: 构建失败？

A: 删除 `node_modules` 和 `dist` 目录，重新运行 `npm install`。

---

## 架构分析

项目经过了全面的架构分析和优化，详见 [ARCHITECTURE_ANALYSIS.md](ARCHITECTURE_ANALYSIS.md)。

### 优化成果

- ✅ 事件驱动架构 - 系统解耦
- ✅ 依赖注入 - 自动化管理
- ✅ 空间分区 - 性能优化
- ✅ 协调器系统 - 职责分离
- ✅ 统一配置 - 集中管理
- ✅ 性能监控 - 实时跟踪

---

## 贡献指南

欢迎贡献！请遵循以下步骤：

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: Add AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范

- 使用 TypeScript 类型注解
- 遵循现有代码风格
- 添加适当的注释
- 更新相关文档

---

## 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。

---

## 致谢

- [Three.js](https://threejs.org/) - 优秀的 3D 渲染库
- [Cannon-es](https://github.com/pmndrs/cannon-es) - 物理引擎
- [Kenney.nl](https://kenney.nl/) - 游戏资产
- [Quaternius](https://sketchfab.com/quaternius) - 敌人模型

---

**Happy Coding! 🎮**
