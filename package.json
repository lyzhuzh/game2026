{
  "name": "threejs-fps-game",
  "version": "1.0.0",
  "type": "module",
  "description": "A first-person shooter game built with Three.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "mixamo:generate": "ts-node scripts/mixamo-downloader.ts generate",
    "mixamo:download": "ts-node scripts/mixamo-downloader.ts download",
    "mixamo:organize": "node scripts/organize-mixamo-downloads.js",
    "organize:assets": "npm run mixamo:organize"
  },
  "dependencies": {
    "three": "^0.160.0",
    "cannon-es": "^0.20.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "ts-node": "^10.9.0",
    "puppeteer": "^21.0.0"
  }
}
